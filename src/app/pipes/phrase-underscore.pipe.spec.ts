import { PhraseUnderscorePipe } from './phrase-underscore.pipe';

describe('PhraseUnderscorePipe', () => {
  it('create an instance', () => {
    const pipe = new PhraseUnderscorePipe();
    expect(pipe).toBeTruthy();
  });
});
