import { AssimilatePromptPipe } from './assimilate-prompt.pipe';

describe('AssimilatePromptPipe', () => {
  it('create an instance', () => {
    const pipe = new AssimilatePromptPipe();
    expect(pipe).toBeTruthy();
  });
});
