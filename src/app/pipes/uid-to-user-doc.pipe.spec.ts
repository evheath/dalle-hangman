import { UidToUserDocPipe } from './uid-to-user-doc.pipe';

describe('UidToUserDocPipe', () => {
  it('create an instance', () => {
    const pipe = new UidToUserDocPipe();
    expect(pipe).toBeTruthy();
  });
});
