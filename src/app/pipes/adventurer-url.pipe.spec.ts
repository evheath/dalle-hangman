import { AdventurerUrlPipe } from './adventurer-url.pipe';

describe('AdventurerUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new AdventurerUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
